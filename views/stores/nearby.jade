extends ../layouts/default

block main
  h1= title

block content
  - if (stores.length)
    - each store in stores
      include store

    - if (pages > 1)
      ul.pagination
        != createPagination(pages, page)
  - else
    h4.muted No nearby stores were found.&nbsp;
